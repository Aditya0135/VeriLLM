# Paths only. Hyperparameters live in params.yaml.
artifacts_root: artifacts

data_ingestion:
  root_dir: artifacts/data_ingestion

reference_retrieval:
  root_dir: data
  reference_map_path: data/reference_map.json

annotation:
  root_dir: data
  # NEW file. The 6-prompt ensemble in data/annotations.jsonl is never
  # written to - it is the control arm of the prompt ablation.
  annotations_path: data/annotations_strict.jsonl
  log_path: artifacts/logs/annotation.log

label_aggregation:
  root_dir: artifacts/label_aggregation
  dataset_path: artifacts/label_aggregation/silver_dataset

model_trainer:
  root_dir: artifacts/model_trainer
  checkpoint_dir: artifacts/model_trainer/checkpoints
  final_model_dir: artifacts/model_trainer/final

model_evaluation:
  root_dir: artifacts/model_evaluation
  metrics_path: artifacts/model_evaluation/metrics.json
  plots_dir: artifacts/model_evaluation/plots
